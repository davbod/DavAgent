# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Environment variables
.env

# Raw salvaged agent-workflow output: large, noisy, unsynthesized. The findings
# worth keeping are distilled into docs/specs/ instead.
.workflow-research/

# Library search index: regenerable from library/*.md via
# tools.library.rebuild_library_index(); ignored because it holds raw
# embedding vectors that would otherwise churn on every single save.
library/index.json

# IDE / Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Jarvis voice front-end venv (see requirements-jarvis.txt)
.venv-jarvis/
